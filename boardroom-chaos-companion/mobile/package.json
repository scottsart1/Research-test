{
  "name": "boardroom-chaos-companion-mobile",
  "version": "1.5.0",
  "private": true,
  "description": "Capacitor wrapper that packages the Boardroom Chaos Companion web app as Android and iOS apps.",
  "scripts": {
    "sync": "cap sync",
    "sync:android": "cap sync android",
    "sync:ios": "cap sync ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "apk:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "apk:release": "cap sync android && cd android && ./gradlew assembleRelease",
    "apk:copy": "cp android/app/build/outputs/apk/debug/app-debug.apk releases/boardroom-chaos-companion-v1.5.0-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
